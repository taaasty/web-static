import React, { PropTypes } from 'react';

function FlowsListItem() {
  return (
    <div />
  );
}

FlowsListItem.displayName = 'FlowsListItem';

FlowsListItem.propTypes = {
  flow: PropTypes.object.isRequired,
};

export default FlowsListItem;
