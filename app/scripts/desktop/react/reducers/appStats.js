import createReducer from './createReducer';

const initialState = {
  data: {},
  isFetching: false,
  error: null,
};

const actionMap = {
  
};

export default createReducer(initialState, actionMap);
